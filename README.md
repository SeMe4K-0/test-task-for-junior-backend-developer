# Мои дополнения

ниже я оставил исходный рэдми файл на всякий случай

добавил тела запросов в swagger, и конкретно периодов, то есть даты, я вставил сразу несколько вариаций, чтобы было наглядно какие и как использовать, но сервер принимает только один вариант, например только monthly, то есть и monthly и daily он принять сразу не сможет. Их можно менять, все работает, и удалять и также сервер отдает полностью тело запроса, у периодов нету определенного типа данных, так как там и массивы и числа и строки, поэтому бэкенд читает как json строку с данными. все где я что либо менял во всех файлах оставил комментарии

# Мои предположения

очень не помешала бы дополнительная валидация, но базовая есть чтобы пользователь не вписал сове имя вместо числа, конкретную дату принимает определенный формат он есть в swagger, я хотел сделать чтобы был общепринятый шаблон допустим 14.02.2007, но с ним возникали ошибки, на которые я не зотел тратить время, затем я бы разобрал вопрос с датами, так как есть ограничения до 30, хотя в других месяцах есть 31, и в феврале 29 и там не все 30 дней, также учитывать праздники, 9 мая условно, то что допустим в такие даты не клиентов не принимают. также сделал бы эту функцию опциональной, может кому-то эта фича и не нужна, также сделал бы напоминалку, трекер который отслеживает, что исполняемая дата скоро исполнится и что-то в этом роде, недалеко отходя добавил бы трекер который отслеживает процесс выполненной задачи, то есть разбить этапы и некий чек лист, также можно сделать чекбокс якобы выполнено или отменено или перенесено для архива

# Task Service

Сервис для управления задачами с HTTP API на Go.

## Требования

- Go `1.23+`
- Docker и Docker Compose

## Быстрый запуск через Docker Compose

```bash
docker compose up --build
```

После запуска сервис будет доступен по адресу `http://localhost:8080`.

Если `postgres` уже запускался ранее со старой схемой, пересоздай volume:

```bash
docker compose down -v
docker compose up --build
```

Причина в том, что SQL-файл из `migrations/0001_create_tasks.up.sql` монтируется в `docker-entrypoint-initdb.d` и применяется только при инициализации пустого data volume.

## Swagger

Swagger UI:

```text
http://localhost:8080/swagger/
```

OpenAPI JSON:

```text
http://localhost:8080/swagger/openapi.json
```

## API

Базовый префикс API:

```text
/api/v1
```

Основные маршруты:

- `POST /api/v1/tasks`
- `GET /api/v1/tasks`
- `GET /api/v1/tasks/{id}`
- `PUT /api/v1/tasks/{id}`
- `DELETE /api/v1/tasks/{id}`
